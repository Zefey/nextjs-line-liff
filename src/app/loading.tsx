'use client'

import { AutoCenter, DotLoading } from 'antd-mobile'

const Loading = () => {
  return (
    <AutoCenter>
      <DotLoading color="primary" />
    </AutoCenter>
  )
}

export default Loading
